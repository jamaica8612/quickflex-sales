# 플렉스노트 디자인 규칙 — PWA · 2026-09

이 문서는 플렉스노트 PWA의 **유일한 디자인 기준**입니다. 새 화면을 만들거나 기존 화면을 고칠 때 여기 값을 쓰세요. 값을 바꾸고 싶으면 이 문서를 먼저 고치고 코드를 따라가게 하세요. 임시 비교 도구, 과거 CSS 선언, 개별 화면의 관성적인 값은 이 문서와 충돌할 때 기준이 아닙니다.

관련 문서: 인수인계와 남은 작업은 [`HANDOFF.md`](./HANDOFF.md)

---

## 0. 이 디자인이 푸는 문제

원래 앱은 "촌스럽다"는 평을 받았고, 원인은 자간이나 여백이 아니라 셋이었습니다.

| 원인 | 해법 |
|---|---|
| 글자가 전부 굵었다 (`650`~`900` 일곱 단계) | **네 단계로 고정** |
| 모든 요소에 1px 테두리 → 전부 같은 층에 눌러붙어 평평함 | **표면 밝기와 그림자로 층을 만든다** |
| 강조색이 쨍했다 (다크 `#FFD24A`) | **채도를 낮춘다** |

**이 세 가지를 되돌리면 리디자인이 무의미해집니다.** 새 컴포넌트를 만들 때도 이 셋을 먼저 확인하세요.

---

## 1. 색

### 라이트 (`:root`)

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `--bg` | `#F5F5F5` | 화면 바탕 |
| `--panel` | `#FFFFFF` | 카드 |
| `--panel2` | `#F7F7F8` | 카드 안 보조면, 세그먼트 트랙 |
| `--panel3` | `#F4F4F5` | 진행바·막대 트랙 |
| `--line` | `rgba(112,115,124,.18)` | 구분선, 달력 괘선 |
| `--line-strong` | `#DBDCDF` | 부 버튼 테두리 |
| `--text` | `#171719` | 본문 |
| `--muted` / `--soft` | `#70737C` | 라벨, 보조 |
| `--gold` | `#1B62D6` | **강조** |
| `--gold2` | `#3B82E8` | 그라데이션 짝 |
| `--red` | `#DC3545` | 일요일, 휴무, 경고 |
| `--blue` | `#1A73E8` | 토요일 |
| `--green` | `#28A745` | 완료 |
| `--button-secondary` | `var(--panel2)` | 보조 버튼 표면 |
| `--button-secondary-text` | `var(--text)` | 보조 버튼 글자 |
| `--button-secondary-border` | `var(--line)` | 필요한 보조 버튼 구분선 |

### 다크 (`[data-theme="dark"]`)

| 토큰 | 값 |
|---|---|
| `--shell-bg` | `#101010` |
| `--bg` | `#141414` |
| `--panel` | `#1C1C1C` |
| `--panel2` | `#242424` |
| `--panel3` | `#2C2C2C` |
| `--line` | `rgba(255,255,255,.075)` |
| `--line-strong` | `rgba(255,255,255,.40)` |
| `--text` | `#ECEEF2` |
| `--muted` | `#8A9099` |
| `--soft` | `#929292` |
| `--gold` | `#E8B85C` |
| `--gold2` | `#C99A4A` |
| `--red` | `#FF7B85` |
| `--blue` | `#7EA7F7` |

### 색 규칙

1. **하드코딩 hex 금지.** 새 규칙에 `#`으로 시작하는 색을 쓰지 마세요. 반드시 토큰을 거칩니다. 확인: `grep -nE "#[0-9a-fA-F]{3,8}" styles.css` 결과가 `:root`·`[data-theme]` 블록 안으로만 나와야 합니다.
2. **강조색은 두 곳에만** — 지금 상태를 말하는 값(정산 예상액, 진행률), 주 행동 버튼(기록하기, 점검하기 아님). 한 화면에 유채색 3곳 이하.
3. **다크의 회색에 색조를 넣지 않는다.** 배경과 표면은 RGB가 같은 중립 차콜을 씁니다. 홈 헤더는 강조색 그라데이션 없이 `--bg` 단색으로 이어집니다. 라이트도 홈 바탕은 단색이며 바깥 배경은 `#ECECEC`입니다.
4. **모드별로 강조색이 다르다.** `--gold` 하나가 라이트에선 파랑, 다크에선 황동입니다. 이 비대칭은 의도된 것이니 통일하지 마세요.
5. 새 색이 필요하면 `color-mix(in srgb, var(--gold) N%, transparent)` 로 파생시킵니다.

---

## 2. 글꼴

| 역할 | 토큰 | 글꼴 |
|---|---|---|
| 한글·본문 | `--font-ui` | **IBM Plex Sans KR** |
| 모든 수치 | `--font-numeric` / `--font-amount` | **Archivo** → IBM Plex Sans KR 폴백 |
| 코드·시각·기간 | `--font-code` | **JetBrains Mono** → IBM Plex Sans KR 폴백 |

셋 다 SIL OFL 1.1. 제품 화면은 Google Fonts `@import`로 불러오는 이 세 가족과 역할 토큰을 사용합니다. 기존 소개 자료의 Pretendard·Wanted Sans 번들 및 라이선스는 해당 자료를 따로 정리할 때까지 보존하며, 제품 화면의 새 글꼴 기준으로 사용하지 않습니다.

### 굵기 — 네 단계만

| 단계 | 쓰는 곳 |
|---|---|
| **500** | 라벨, 보조 텍스트, 비활성 탭 |
| **600** | 카드 제목(muted), 부 버튼, 세그먼트 |
| **700** | 값, 주 버튼, 활성 탭, 제목 |
| **800** | 가장 큰 히어로 숫자의 최대 강조 |

홈 정산 예상액은 34px/700으로 사용합니다. 히어로라는 이유로 모든 큰 수치를 800으로 올리지 않습니다.

`650` `680` `750` `760` `850` `900` 은 **쓰지 않습니다.**

### 자간

| 크기 | 자간 |
|---|---|
| 30px 이상 (히어로 숫자) | `-0.035em` |
| 15~26px (값) | `-0.022em` ~ `-0.028em` |
| 본문 | `0` |
| 10px 대문자/트래킹 라벨 | `+0.12em` |
| 모노 | `+0.01em` |

### 필수 규칙

- **모든 수치에 `font-variant-numeric: tabular-nums`.** 자릿수가 바뀌어도 안 흔들립니다.
- **단위는 한 단계 작게 + muted.** `4,182,680`은 크게, `원`은 작고 흐리게. 숫자 덩어리가 먼저 읽혀야 합니다.
- **자릿수가 고정된 문자열은 모노.** `정산기간 8/26 - 9/25`, `09/13`, 구역코드 `302B`.

---

## 3. 표면과 층

테두리로 나누지 않고 **표면 밝기 + 그림자**로 층을 만듭니다.

| 층 | 배경 | 테두리 | 그림자 |
|---|---|---|---|
| 화면 | `--bg` | — | — |
| 카드 | `--panel` | 없음이 기본 | `--shadow-card` |
| **주 카드** (정산 예상액) | `--panel` + 위→아래 미세 그라데이션 | `1px --line` | `--shadow-raise` + 안쪽 상단 하이라이트 |
| 카드 안 보조면 | `--panel2` | 없음 | 없음 |
| 트랙 (진행바·막대) | `--panel3` | 없음 | 안쪽 그림자 |

```css
--shadow-card:  0 1px 2px rgba(16,24,40,.05), 0 10px 28px -14px rgba(16,24,40,.16);
--shadow-raise: 0 1px 2px rgba(16,24,40,.05), 0 18px 40px -20px rgba(16,24,40,.22);
```

### 주 카드가 존재감을 얻는 방법

다크에서는 그림자가 거의 안 보여 카드가 배경에 묻힙니다. 그래서 세 겹을 씁니다.

```css
box-shadow:
  inset 0 1px 0 rgba(255,255,255,.07),   /* 윗면이 빛을 받은 것처럼 */
  0 2px 6px -2px rgba(0,0,0,.30),         /* 가까운 그림자 */
  0 22px 44px -22px rgba(0,0,0,.55);      /* 깊은 그림자 */
background: linear-gradient(180deg, color-mix(in srgb,#FFFFFF 4%, var(--panel)) 0%, var(--panel) 42%);
```

### 선을 남기는 예외

**표의 구조를 나타내는 선은 남깁니다.** 달력 괘선(`.day-cell` 의 `border-right`/`border-bottom`), 4지표 사이 세로선, 리스트 행 사이 가로선. 이건 장식이 아니라 정보입니다.

---

## 4. 형태와 간격

| 항목 | 값 |
|---|---|
| 주 카드 radius | 20px |
| 일반 카드 radius | 16~18px |
| 버튼·칩 radius | 999px (알약) |
| 세그먼트 트랙 radius | 999px, 안쪽 버튼 999px |
| 화면 좌우 여백 | 14~18px |
| 카드 안쪽 여백 | 18px (좌우), 상단 20px |
| 카드 사이 | 10~12px |
| 구분선 | 1px `--line` |

### 터치 크기 — 역할에 따라 다르게

| 역할 | 최소 높이 |
|---|---|
| 주 행동 (기록하기, 저장) | **44px** |
| 부 행동 (휴무, 기록 수정, 오늘) | **30~40px** |
| 세그먼트 (매출/건수) | **26~32px** |

**전부 44px로 만들지 마세요.** 원래 앱의 문제였습니다. 크기가 같으면 무엇이 주된 행동인지 드러나지 않습니다.

### 폼과 나란한 버튼 — 역할별 확정값

아래 값은 설정과 지출 화면을 포함한 새 폼의 기본값입니다. 화면별 CSS에서 같은 역할을 다른 값으로 다시 정의하지 않습니다.

| 역할 | 높이 | 글자 | 굵기 | 비고 |
|---|---:|---:|---:|---|
| 설정 필드 라벨 | — | 12px | 500 | `--muted`, 줄높이 20px |
| 일반 `input`·`select` | 44px | 14px | 500 | 본문은 `--font-ui`, 수치는 `--font-numeric`, 날짜는 `--font-code` |
| 일반 placeholder | — | 14px | 500 | `--muted`, opacity 1 |
| 지출 `총 금액` 입력 | 52px | 20px | 600 | 오른쪽 단위 공간을 확보하고 수치는 `--font-numeric` |
| 지출 `총 금액` placeholder | — | 14px | 500 | 큰 입력값과 placeholder의 위계를 구분 |
| 입력 안 단위 (`원` 등) | — | 14px | 500 | `--muted`, 숫자와 겹치지 않게 고정 여백 확보 |
| 폼 하단 주·부 버튼 | 44px | 14px | 600~700 | 같은 결정의 한 쌍은 높이·글자 크기를 동일하게 유지 |

같은 행에서 같은 결정을 이루는 병렬 버튼은 주·부 색상과 표면으로 위계를 만들고, 높이와 글자 크기는 일치시킵니다. 굵기는 600·700 중 역할에 맞게 사용합니다. 예를 들어 `임시 저장 / 지출 저장`은 둘 다 `44px · 14px · 600`입니다. 파일 선택처럼 별도의 보조 행동끼리 짝을 이루는 경우에도 두 버튼은 동일한 규격을 사용합니다.

설정의 접힌 그룹 `summary`가 이미 항목 이름을 말하면 내부에 같은 제목을 반복하지 않습니다. `내 정보 > 내 정보`처럼 동일한 제목은 제거하고, `근무·단가 > 단가 관리`나 `화면 > 테마`처럼 실제 하위 구분이 필요할 때만 내부 제목·라벨을 둡니다.

---

## 5. 모션

| 대상 | 값 |
|---|---|
| 목표 진행바 채움 | `width .4s ease` |
| 진행바 광택 스윕 | `4.2s cubic-bezier(.4,0,.2,1) infinite`, 58%까지 대기 후 통과 |
| 버튼 누름 | `transform: scale(.98)`, 120ms |

`prefers-reduced-motion: reduce` 에서 광택은 `opacity: 0` + `animation: none`.

**애니메이션은 성취를 말할 때만.** 목표 진행바가 유일한 예외입니다.

---

## 6. 컴포넌트

### 6-1. 정산 예상액 카드 (주 카드)

이 화면의 주인공. 순서를 바꾸지 마세요.

```
정산 예상액              [이번 정산기간]
4,182,680원                        ← 34px / 800 / −0.035em / --gold
━━━━━━━━━━━━━━━━━         69.7%    ← 진행바 + 퍼센트
       목표 600만원 대비 진행률       ← 10.5px muted 가운데
─────────────────────────────
배송건수  평균건수  일평균  근무일     ← 4열, 사이 세로선
4,982건   189건  158,800원  26일
─────────────────────────────
일상점검  운행 전 작성 의무  [점검하기]  ← 무채색, 카드 아래 모서리
```

### 6-2. 목표 진행바

이 화면에서 유일하게 **성취**를 말하는 요소라 힘을 줍니다.

```css
height: 9px; border-radius: 999px;
background: var(--panel3);
box-shadow: inset 0 1px 2px rgba(0,0,0,.16);
```

채움은 6스톱 그라데이션 + 글로우 + 안쪽 상단 하이라이트, 그 위에 4.2초마다 흰빛이 한 번 훑고 지나갑니다. 자세한 값은 `styles.css` 섹션 13.

**다른 막대에 이 처리를 복사하지 마세요.** 여러 개가 반짝이면 아무것도 안 보입니다.

### 6-3. 일상점검 줄

**주인공이 아닙니다.** 매일 한 번 하고 끝나는 의무 항목이라 색을 주면 정산액과 경쟁합니다.

- 배경 투명, 위쪽 `1px --line`
- "일상점검" muted 600, "운행 전 작성 의무" muted 500 opacity .72
- 버튼은 투명 + `--line-strong` 테두리
- 완료 상태도 초록 대신 무채색 — 색이 아니라 문구로 알립니다

### 6-4. 달력

```
[오늘]      ‹   2026년 9월   ›      [매출|건수]
```

- 달 이름은 **화면 정중앙 절대 정렬** (`position:absolute; left:50%; transform:translateX(-50%)`)
- 화살표는 이름 기준 `±82px` 절대 위치 → 이름 길이가 바뀌어도 간격 유지
- `오늘`은 왼쪽 끝, `매출/건수`는 오른쪽 끝
- 360px 이하에서 `오늘` 자동 숨김
- 행 높이 78px, 셀은 `날짜(24px 원) / 금액 / 라우트` 3단
- 라우트는 `compactRouteList`로 압축(`319A 319B 319C 319D` → `319ABCD`), 한 줄 말줄임
- 일요일·공휴일 `--red`, 토요일 `--blue`, 오늘은 강조색 원 배지

### 6-5. 리스트 행 (구역별 매출)

- 상자를 두르지 않고 **행 사이 괘선**만
- 3열: 구역(모노 700) · 근거(모노 muted, `62개`) · 금액(우측 정렬, Archivo 700)
- **행 높이 62px 고정.** 세부구역 줄이 있는 행과 없는 행이 섞여 들쭉날쭉했습니다
- 세부구역 줄은 한 줄 말줄임

### 6-6. 세그먼트 컨트롤

```css
트랙: --panel2, radius 999px, padding 2~3px
버튼: 26~32px 높이, 투명, muted 600
활성: --panel3(다크) / #FFFFFF(라이트), --text, 700, box-shadow 살짝
```

**활성 상태는 트랙보다 밝아야 '올라온' 것으로 읽힙니다.** 다크에서 `--panel`을 쓰면 트랙보다 어두워져 구멍처럼 보입니다.

### 6-7. 요일별 막대

- **합계가 아니라 근무일당 평균.** 합계로 하면 근무일이 많은 요일이 무조건 이겨서 "토요일이 좋다"가 아니라 "토요일에 많이 일했다"만 보입니다
- 각 막대 아래 **표본(근무일수)** 표시 — 4일치로 단정하지 않게
- 최고 요일만 강조색, 나머지는 `--gold 34%` 톤
- 맨 아래 한 줄로 최고-최저 차이를 문장으로

### 6-8. 하단 내비

아이콘은 **라벨이 말하는 것**을 그려야 합니다.

| 탭 | 아이콘 |
|---|---|
| 달력 | 달력 (날짜 칸 있는) — 집 아이콘 아님 |
| 측정 | 계기 (반원 아크 + 바늘) — 막대그래프 아님 |
| 통계 | 막대그래프 |
| 설정 | 톱니 |

측정과 통계가 둘 다 막대그래프였던 게 원래 문제였습니다. 21px, stroke 1.8~1.9.

---

### 6-9. 일상점검 서명

- 설정에서는 설명과 `서명 등록 / 변경` 버튼 한 줄만 표시합니다.
- 버튼을 누르면 모바일은 하단 팝업, 큰 화면은 최대 560px 모달에서 손글씨를 입력합니다. 서명칸은 가로형 3:2 비율이며 팝업은 내용 높이만 차지합니다.
- 흰 서명 영역, `다시 쓰기`·`닫기` 보조 버튼, `서명 저장` 주 버튼을 사용합니다.
- 이름으로 서명 이미지를 생성하는 옵션은 제거했습니다. 이미 저장된 과거 점검 기록은 유지합니다.
- 빈 서명은 저장하지 않으며 실패 시 그린 내용은 유지합니다. 닫으면 미저장 입력은 버립니다.

`?palette-preview=1`은 loopback 환경의 임시 진단 도구일 뿐이며 디자인 기준이나 배포 기능이 아닙니다. 후보 이름과 개수는 실험 중 바뀔 수 있으므로 이 문서에 후보 목록을 고정하거나 후보 토큰을 제품 CSS에 복사하지 않습니다. 최종 색은 이 문서 1절의 라이트·다크 토큰만 따릅니다.

### 6-10. 기사 통계

- 기본 순서는 **누적 매출 → 같은 근무일수의 지난 정산 비교 → 평소 하루 매출과 요일 패턴 → 정산 내역**입니다. 구역 등록 여부와 무관하게 매출·근무일 기록을 중심으로 읽습니다.
- 가장 큰 숫자는 선택 기간의 누적 매출입니다. 목표가 있으면 남은 금액과 달성률을 보조로 표시합니다.
- 물량·단가·수당 비교, 구역 상세, 날짜 목록은 접어서 시작합니다. 매출 흐름은 내 매출 패턴 위에 항상 표시합니다. 구역 기록이 없으면 구역 섹션을 숨깁니다.
- 물량 없는 근무일을 빠뜨리지 않습니다. 물량 근거가 불완전한 비교 집합에서는 물량·단가를 0으로 대체하지 않고 숨깁니다.
- 평소 하루 매출은 근무일 매출의 중앙값입니다. 4일 이상이면 가운데 50% 범위를 함께 표시합니다.
- 일정 기반 전망은 등록된 남은 일정이 있을 때 선택해서 봅니다. 미래 일정에 빈 날짜가 있거나 오늘 기록이 미완료면 최종 예상액을 만들지 않습니다. 예측에는 최소 3일의 실제 근무 기록이 필요합니다.
- 정산 내역 합계는 날짜별 확정 계산값을 더합니다. 구역별 반올림 결과를 다시 합쳐 원 단위 오차를 만들지 않습니다.
- 정산 제목 옆에는 합계를 반복하지 않습니다. 내역 행은 44px, 항목 13px·금액 16px·단위 12px로 맞추고 마지막 합계만 강조색을 씁니다.
- 요일 막대는 최대 18px 폭·96px 트랙으로 두고 미세한 세로 그라데이션을 사용합니다. 요일·평균은 12px, 근무일수는 11px입니다.
- 매출 흐름은 높이 112px의 작은 선과 옅은 면으로 표현합니다. 휴무·미기록 구간은 제외하고 실제 근무 기록만 순서대로 연결하며 실제 0원 근무 기록은 유지합니다. 양끝에 날짜·매출을 작게 붙이고 일 단위 조회는 첫 근무일 대비 증감을 표시합니다. 장기 조회는 주별·정산별 합계임을 표시하며 부분 구간끼리 증감률을 비교하지 않습니다. 축·눈금·평균선·점·툴팁·지표 전환은 두지 않습니다.
- 시급·순수익은 표시하지 않습니다. 총 업무시간과 비용 입력을 전제로 하는 별도 기능입니다.

### 가로 화면의 달력

- 가로 768px 이상 또는 데스크톱 1100px 이상에서 상단 요약, 왼쪽 달력, 오른쪽 선택 날짜 패널을 사용합니다. 그보다 좁은 화면과 태블릿 세로 화면은 기존 한 단 흐름을 유지합니다.
- 날짜 패널은 휴무·미기록·직접 입력·자동 측정일 모두 유지합니다. 구역을 등록하지 않은 기사도 금액과 기록 행동을 같은 위치에서 확인합니다.
- 근무표만 있는 날은 매출 미기록으로 표시합니다. 완료된 자동 기록의 실제 0원과 구분하며, 계산이나 저장 규칙은 바꾸지 않습니다.
- 가로 화면의 기록·휴무 버튼은 날짜 패널 안에 모읍니다. 화면 전체를 가로지르는 하단 독은 사용하지 않고, 메뉴는 왼쪽 세로 레일로 배치합니다.
- 달력의 날짜·매출·구역명은 셀 안에 모두 들어가야 합니다. 긴 구역명은 한 줄 말줄임하되 아래쪽을 잘라내지 않습니다.
- 좁은 가로 화면에서는 요약을 두 줄로 접어 필수 수치를 보존합니다. 공간을 맞추려고 글자를 과도하게 작게 만들지 않습니다.

## 7. 글쓰기

- **숫자 뒤 단위는 작게.** `4,982` + `건`(작게 muted)
- **판단을 대신 써준다.** 숫자만 늘어놓지 말고 `토요일이 월요일보다 근무일당 58,000원 많습니다` 처럼 문장으로
- **표본을 밝힌다.** `4일 표본` 처럼 근거가 얼마나 되는지 적습니다
- **틀렸다고 말하지 않는다.** `감지보다 +3개` 처럼 차이만 보여줍니다
- 버튼은 일어날 일을 그대로 씁니다 — `기록하기`, `저장`

---

## 8. 새 화면 만들 때 체크리스트

- [ ] 하드코딩 hex 없음 (`grep -nE "#[0-9a-fA-F]{3,8}" styles.css`)
- [ ] 굵기가 500·600·700·800 중 하나
- [ ] 모든 수치에 `tabular-nums`, 숫자는 `--font-numeric`
- [ ] 단위가 숫자보다 작고 muted
- [ ] 설정 라벨은 12px/500, 일반 필드는 44px·14px/500
- [ ] 지출 총 금액 입력은 52px·20px/600, placeholder와 단위는 14px/500
- [ ] 같은 결정의 병렬 버튼은 높이·글자 크기가 같음
- [ ] 설정 그룹 제목을 내부에서 반복하지 않음
- [ ] 유채색이 3곳 이하
- [ ] 주 버튼만 44px, 부 버튼은 낮음
- [ ] 카드가 꼭 필요한가? (선이나 여백으로 되면 카드를 쓰지 않는다)
- [ ] 라이트·다크 양쪽 확인
- [ ] 360px 폭에서 가로 스크롤 없음
- [ ] `prefers-reduced-motion` 대응

---

## 9. 안드로이드 측정 앱에 옮길 때

두 앱이 같은 시스템을 쓰는 것이 목표입니다. 토큰 이름을 맞추세요.

| PWA | Compose |
|---|---|
| `--bg` | `ground` |
| `--panel` | `surface` |
| `--panel2` | `surfaceVariant` |
| `--line` | `line` |
| `--text` | `text` |
| `--muted` | `muted` |
| `--gold` | `accent` |
| `--red` | `stateBad` |
| `--blue` | `stateInfo` |
| `--green` | `stateOk` |

- IBM Plex Sans KR·Archivo·JetBrains Mono를 `res/font/`에 번들하고 OFL 원문을 `assets/licenses/`에 함께 넣습니다.
- 굵기 네 단계, 자간, 단위 처리 규칙은 그대로 적용
- 같은 구역은 두 앱에서 같은 색이어야 합니다


### 2026-09-13 검수 적용 메모
- 캘린더 연동은 설정 안에서 제공하며, 주 동기화 버튼 44px/13px, 별도 보조 행동 36px/13px를 사용합니다. 같은 결정을 이루는 폼 하단 쌍과 구분합니다.
- 상태 갱신으로 입력 DOM을 다시 만들지 않습니다. 비동기 완료·실패를 상태 문구로 알리고 키보드 초점을 유지합니다.
- 새 개인정보처리방침·소개 링크도 같은 색상과 글꼴 역할을 사용합니다. 과거 문서의 색 후보를 제품 토큰으로 복원하지 않습니다.
