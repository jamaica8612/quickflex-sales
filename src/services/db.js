// Database implementation currently lives in src/main.js while the legacy
// single-file app is being separated. Keep this file as the stable home for
// Supabase CRUD, RLS-aware queries, and admin aggregate reads.
export const dbModule = "Supabase CRUD and admin queries";
